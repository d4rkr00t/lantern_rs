[package]
name = "lantern_dependency_graph"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
lantern_symbols_map = { path = "../lantern_symbols_map" }
